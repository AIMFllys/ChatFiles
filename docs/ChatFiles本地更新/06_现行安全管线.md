# 06 · 现行安全管线（2026-07）

本页是当前唯一可执行的本地更新入口。00-05 继续解释高水位、边界与历史背景，但其中的旧路径和旧脚本不得直接运行。

## 1. 不变的红线

- 原始微信、QQ 数据只读；所有解密、解析和提炼只写版本副本。
- 禁止手工覆盖或删除 `data/insights`、`data/wechat.current`、`archive` 中的既有成果；只允许审计通过后的正式激活流程做版本化移动与替换。
- `data/`、`archive/`、`work/`、`imports/`、密钥和身份配置永不进入 Git。
- QQ 正文未解密、微信 `.dat` 未物化、语音导出失败等必须保留真实失败状态，禁止用占位内容伪装成功。
- 任一 audit 未通过时禁止激活；失败候选和 previous 目录保留用于审计，不批量清理。

## 2. 微信版本快照与规范数据库

在线微信库必须通过原生 SQLCipher helper 生成版本快照。scanner 与 helper 必须使用已验收的明确路径，禁止按模糊文件名挑选旧诊断版本。

```powershell
npx tsx scripts/snapshotLiveWeChat.ts `
  --pid {{微信主进程PID}} `
  --account-root "{{精确账号根}}" `
  --output-root work/snapshots/live `
  --scanner "{{wechat-key-scanner.exe}}" `
  --snapshot-helper "{{sqlcipher-snapshot-helper.exe}}" `
  --run-id {{唯一run-id}}
```

只有 snapshot manifest 末态为 `validated`、`snapshot-runs.jsonl` 同 run 为 `complete` 的目录才可进入解析源。发布到 `work/decrypted/wechat/<run-id>` 前，应先在同盘 staging 中逐文件核对相对路径和 SHA-256，再 rename；不要让 `.incoming-*` 进入 parser source root。

```powershell
$run = '{{唯一run-id}}'
$env:CHATFILES_RUN_ID = $run
npx tsx scripts/parseWeChat.ts
npm run audit:chat
```

解析器只发布 `data/wechat.next`。`npm run audit:chat` 必须完成 source/output 行级身份闭合、规范 owner、消息类型和 UTF-8 来源核对。激活聊天库前停止本地服务。仓库尚无 chat/assets activate CLI；确认审计通过后，只能用下面的受控同盘 rename，禁止自行简化成覆盖或删除：

```powershell
function Read-StrictJson([string]$path) {
  $utf8 = [Text.UTF8Encoding]::new($false, $true)
  $text = $utf8.GetString([IO.File]::ReadAllBytes($path))
  return $text | ConvertFrom-Json
}

function Assert-PlainDirectory([string]$path) {
  if (-not (Test-Path -LiteralPath $path -PathType Container)) { throw "目录不存在: $path" }
  $item = Get-Item -LiteralPath $path -Force
  if (($item.Attributes -band [IO.FileAttributes]::ReparsePoint) -ne 0) { throw "拒绝 reparse point: $path" }
}

function Move-AuditedBundle([string]$name, [string]$runId) {
  if ($name -notin @('wechat', 'chat-assets')) { throw 'bundle 名称不在允许列表' }
  if ($runId -notmatch '^[0-9A-Za-z._-]+$') { throw 'run-id 含不安全字符' }
  $dataItem = Get-Item -LiteralPath data -Force
  if (($dataItem.Attributes -band [IO.FileAttributes]::ReparsePoint) -ne 0) { throw '拒绝 reparse point: data' }
  $data = $dataItem.FullName
  $current = Join-Path $data "$name.current"
  $next = Join-Path $data "$name.next"
  $previous = Join-Path $data "$name.previous.$runId"

  foreach ($path in @($current, $next, $previous)) {
    $parent = [IO.Path]::GetDirectoryName([IO.Path]::GetFullPath($path))
    if (-not [string]::Equals($parent, $data, [StringComparison]::OrdinalIgnoreCase)) {
      throw "bundle 路径越界: $path"
    }
  }

  $hasCurrent = Test-Path -LiteralPath $current -PathType Container
  $hasNext = Test-Path -LiteralPath $next -PathType Container
  $hasPrevious = Test-Path -LiteralPath $previous -PathType Container
  if (-not $hasCurrent -and $hasNext -and $hasPrevious) {
    Assert-PlainDirectory $next
    Assert-PlainDirectory $previous
    Move-Item -LiteralPath $previous -Destination $current -ErrorAction Stop
    throw '检测到上次在第一步 rename 后中断，已恢复 current；请重新审计后重跑'
  }
  if ($hasCurrent -and -not $hasNext -and $hasPrevious) {
    Assert-PlainDirectory $current
    Assert-PlainDirectory $previous
    $activeIndex = Read-StrictJson (Join-Path $current 'index.json')
    $activeRunId = [string]$activeIndex.runId
    if ($activeRunId -cne $runId) { throw '激活拓扑异常：current runId 与 previous 名称不一致' }
    Write-Output "已激活，无需重复移动: $current"
    return
  }
  if (-not $hasCurrent -or -not $hasNext -or $hasPrevious) {
    throw '激活拓扑异常，停止人工处理并保留全部目录'
  }

  Assert-PlainDirectory $current
  Assert-PlainDirectory $next
  $indexPath = Join-Path $next 'index.json'
  $index = Read-StrictJson $indexPath
  if ([string]$index.runId -cne $runId) { throw 'candidate index.json 的 runId 与命令不一致' }

  if ($name -eq 'wechat') {
    $auditLines = & npm --silent run audit:chat
  } else {
    $auditLines = & npx tsx scripts/auditConversationAssets.ts --bundle $next
  }
  $auditExitCode = $LASTEXITCODE
  if ($auditExitCode -ne 0) { throw "候选审计失败，退出码: $auditExitCode" }
  $audit = ($auditLines -join "`n") | ConvertFrom-Json
  if ($audit.ok -ne $true) { throw '候选审计未返回 ok: true' }
  if ($name -eq 'wechat') {
    $expectedDatabase = [IO.Path]::GetFullPath((Join-Path $next 'wechat.db'))
    $auditedDatabase = [IO.Path]::GetFullPath([string]$audit.database)
    if ($audit.strict -ne $true -or -not [string]::Equals(
      $expectedDatabase,
      $auditedDatabase,
      [StringComparison]::OrdinalIgnoreCase
    )) { throw 'chat audit 未严格绑定候选数据库' }
  }

  $workItem = Get-Item -LiteralPath work -Force
  if (($workItem.Attributes -band [IO.FileAttributes]::ReparsePoint) -ne 0) { throw '拒绝 reparse point: work' }
  $auditDirectory = Join-Path $workItem.FullName 'audits'
  [IO.Directory]::CreateDirectory($auditDirectory) | Out-Null
  Assert-PlainDirectory $auditDirectory
  $stamp = [DateTime]::UtcNow.ToString("yyyyMMddTHHmmssfffffff'Z'")
  $evidencePath = Join-Path $auditDirectory "$name-activation-$runId-$stamp.json"
  $evidence = [ordered]@{
    version = 1
    bundle = $name
    runId = $runId
    candidateIndexSha256 = (Get-FileHash -LiteralPath $indexPath -Algorithm SHA256).Hash.ToLowerInvariant()
    auditedAt = [DateTime]::UtcNow.ToString('O')
    audit = $audit
  } | ConvertTo-Json -Depth 100
  $bytes = [Text.UTF8Encoding]::new($false, $true).GetBytes($evidence)
  $stream = [IO.File]::Open($evidencePath, [IO.FileMode]::CreateNew, [IO.FileAccess]::Write, [IO.FileShare]::None)
  try { $stream.Write($bytes, 0, $bytes.Length) } finally { $stream.Dispose() }

  Move-Item -LiteralPath $current -Destination $previous -ErrorAction Stop
  try {
    Move-Item -LiteralPath $next -Destination $current -ErrorAction Stop
  } catch {
    if (-not (Test-Path -LiteralPath $current) -and (Test-Path -LiteralPath $previous)) {
      Move-Item -LiteralPath $previous -Destination $current -ErrorAction Stop
    }
    throw
  }
  Write-Output "激活审计证据: $evidencePath"
}

Move-AuditedBundle 'wechat' $run
```

函数会在移动前重新执行对应 audit、校验 candidate `index.json.runId`，并将结果与候选索引 SHA-256 绑定后排他写入 `work/audits/`。激活后保留 `data/wechat.previous.<run-id>`。第二次 rename 失败且 current 仍空缺时，函数会立即恢复 current；失败 bundle 和 previous 都不得删除。

## 3. 洞察候选、闭合与激活

首次修复旧 owner 别名时，先在 `work/audits/` 写显式 alias map。它必须给出唯一 canonical owner、每个旧 owner 到 canonical owner 的映射，以及 strict source audit / 会话覆盖证据。普通后续更新若源洞察已经全是 canonical owner，不需要 alias map。

```json
{
  "version": 1,
  "canonicalOwner": "{{canonical-owner}}",
  "aliases": { "{{legacy-owner}}": "{{canonical-owner}}" },
  "evidence": [{ "kind": "strict-source-identity-audit", "path": "work/audits/{{audit}}.json" }]
}
```

构建候选：

```powershell
$run = '{{唯一run-id}}'

npm run insights:prepare -- `
  --run-id $run `
  --source data/insights

# 仅修复旧 owner 时，改用带证据映射的命令：
npm run insights:prepare -- `
  --run-id $run `
  --source data/insights.previous.{{previous-run-id}} `
  --alias-map work/audits/{{alias-map}}.json

npm run insights:distill -- --run-id $run
npm run insights:boards -- --run-id $run
npm run audit:insights
```

安全契约：

- `prepare` 只读 `data/wechat.current/wechat.db` 与 source，排他发布 `data/insights.next`；source、bundle、数据库必须位于规定角色路径内且不得是 symlink/junction。
- receipt 绑定活动数据库的 SHA-256 与大小、source manifest/state 哈希、alias-map 哈希和 baseline evidence。
- grown 会话使用 `(time, message_uid)` 稳定游标；旧 state 暂无 UID 时仍必须满足 `查询文本行数 == textCount 增长`，否则停止推进。
- `distill` 会读取本轮全部增量文本行，再做有效性筛选和稳定选择。`inputRows / eligibleRows / selectedRows / addedNuggets` 必须写入 receipt 并闭合；不是只读展示出来的几条。
- 旧语义要点只有全部字段相同才去重；`people / date / category / importance` 的差异必须保留。旧 summary 进入 `legacySummaries` 证据，不可静默丢弃。
- distill 在独立 staging 完成后才替换 candidate，并保留 `data/insights.prepared.<run-id>`；boards 同样先完整生成再切换，旧板保留在 candidate 的 `boards.pre-refresh.<run-id>`。
- audit 必须验证当前实质会话集合（本次为 494 个，实际数量以本机为准）、manifest/conv/state 一一对应、baseline evidence 全量存在、游标可解析、全部实际类别板计数闭合（本次为 13 类）、严格 UTF-8 和数据库指纹一致。

激活前停止服务：

```powershell
npm run insights:activate -- --run-id $run
npm run audit:insights -- --bundle data/insights
```

激活保留 `data/insights.previous.<run-id>`，并写 `data/.insights-activation.<run-id>.json`。如果 current 已移动而 candidate 发布失败，命令必须恢复 current 并将 journal 标为 `rolled_back`；异常中断后重跑同一命令会先尝试恢复。

## 4. 会话资产与文件归档

会话资产从已激活的 `data/wechat.current/wechat.db` 与同 run 版本快照构建：

```powershell
npx tsx scripts/buildConversationAssets.ts `
  --source-snapshot work/decrypted/wechat/{{run-id}} `
  --bundle data/chat-assets.next `
  --run-id {{run-id}}

npx tsx scripts/auditConversationAssets.ts --bundle data/chat-assets.next
```

停服、审计通过后，在定义过 §2 的 `Move-AuditedBundle` 的同一 PowerShell 会话中执行 `Move-AuditedBundle 'chat-assets' '{{run-id}}'`。它会保留 `data/chat-assets.previous.<run-id>`，并在第二步失败且 current 仍空缺时恢复 current。随后运行 `npm run audit:assets`；该命令审计的是已激活 current。

文件归档仍是 append-only：

```powershell
$env:CHATFILES_RUN_ID = '{{唯一run-id}}'
npm run ingest:files
```

它只新增经 SHA-256 验证的副本并发布 `data/library.next/{manifest.json,receipt.json}`。当前 UI 和部分脚本仍读取 `data/library.json`；在 reader 迁移或正式 dual-publish 工具完成前，不得宣称 `library.next` 已激活，也不得手工覆盖 `library.json`。

## 5. 最终验收

```powershell
npm test
npm run typecheck:server
npm run build
npm run audit:assets
npm run audit:insights -- --bundle data/insights
git -c core.whitespace=cr-at-eol diff --check
git ls-files data archive work imports secrets
```

必须同时确认：

- full tests 退出码 0；预期平台 skip 可保留但要报告。
- chat / asset / insight audits 均 `ok: true` 且 `issues: []`。
- 若另行做二次 insight delta 幂等复核，必须保存独立计算证据，并确认 `missing=0`、达到阈值的 `grown=0`；小于阈值的 accumulated 可以保留到下轮。`audit:insights` 不会生成这份二次 delta 证据，不得混同两者。
- 浏览器桌面和 390×844 视口无横向溢出，控制台无 warning/error，中文和 Markdown 引用结构正常。
- `git ls-files data archive work imports secrets` 无输出；只提交代码、测试、文档和精确 ignore 规则。
