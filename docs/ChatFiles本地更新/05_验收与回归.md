# 05 · 验收与回归

> [!CAUTION]
> **本页旧计数命令只作历史参考。** 当前验收必须以 [`06_现行安全管线.md`](06_现行安全管线.md) 的 strict chat/asset/insight audits、数据库指纹、文本增长闭合、baseline evidence 和激活 journal 为准；身份去重后总数可以下降，不能再用“只要数量不减”代替来源一致性。

> 增量更新和首次部署最大的不同：除了"新内容补进来了"，还要回归证明"**旧内容没被重复分析、没被破坏**"。两类都过，才算更新成功。

---

## 1. 增量验收（新内容确实补进来了）

```powershell
# 会话/消息总量较更新前增长（或持平，若确无新消息）
python -X utf8 -c "import sqlite3;c=sqlite3.connect(r'data/wechat.db');print('conv/msg =', c.execute('select count(*) from conversations').fetchone()[0], '/', c.execute('select count(*) from messages').fetchone()[0])"

# nugget 总数较更新前增长（或持平）
python -X utf8 -c "import glob,json;print('nuggets=', sum(len(json.load(open(f,encoding='utf-8')).get('nuggets',[])) for f in glob.glob('data/insights/conv/*.json')))"

# 归档文件数较更新前增长（或持平）
node -e "console.log('archived=', require('./data/library.json').stats.archived)"
```

- [ ] 会话 / 消息 / nugget / 归档数 **≥ 更新前**（有新内容则更大）
- [ ] 站点刷新（**无需重启** `npm start`）即可看到新会话 / 新碎金 / 新文件

---

## 2. 回归核对（没重复分析、没破坏旧成果）—— 增量更新的关键

```powershell
# A) 没有实质新增的会话，其 conv/*.json 不应被改动：本轮 delta 之外的文件 mtime 应早于本轮
#    delta 清单：
node -e "console.log(require('./work/insights-delta/_delta.json').map(d=>d.convId))"

# B) conv/*.json 全部仍是合法 JSON（合并没写坏）
python -X utf8 -c "import glob,json;[json.load(open(f,encoding='utf-8')) for f in glob.glob('data/insights/conv/*.json')];print('all-conv-json-valid')"

# C) _state.json 覆盖了所有已分析会话，且 analyzedTextCount 单调（本轮只增不减）
python -X utf8 -c "import json;s=json.load(open('data/insights/_state.json',encoding='utf-8'));print('state entries=',len(s))"
```

- [ ] **delta 之外**的会话：`conv/*.json` 未被改写（没被重复分析）
- [ ] 所有 `conv/*.json` 合法；长大的会话**旧 nugget 仍在**、只多了新 nugget
- [ ] `_state.json` 条目覆盖全部已分析会话，水位只增不减

---

## 3. 安全红线复核（与全项目一致）

- [ ] 重解密只写 `work/decrypted/`，原始库 / 文件**一字未改**
- [ ] 只处理了**你自己的、本机登录账号**的数据；**未上传**任何正文 / 密钥
- [ ] 仍解不了的（QQ 正文、`.dat`、未登录账号）**如实记录**，未伪造
- [ ] 个人数据仍未进 git：`git ls-files data/ archive/ work/ imports/` **无输出**

---

## 4. 清理（可选）

本轮中间产物可留可删（都在 gitignored 的 `work/` 下，留着便于排查）：

```powershell
# 如需清理本轮增量中间文件：
Remove-Item work/chat-digest-delta/* , work/insights-delta/* -Force -ErrorAction SilentlyContinue
# _manifest.prev.json 可保留作上一轮对照，也可删
```

> `data/insights/_state.json` **MUST 保留**——它是下一轮增量更新的高水位基线。

---

## 5. 下一轮

下次再要更新，直接从 [`01_前置与快照.md`](01_前置与快照.md) 开始即可——`_state.json` 已存在，会被当作基线（**首次引导那步自动跳过**），全程同样只补增量、不重复分析。
