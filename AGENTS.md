# AGENTS.md

本文件适用于整个 ChatFiles 仓库。人类的显式指令优先；其余工作必须遵守以下约束。

## 文本与编码

- 所有文本文件统一使用 UTF-8；禁止把文件转换为本地代码页、GBK 或 UTF-16。
- 修改中文前后必须检查 diff，确保中文、emoji、路径和文件名没有乱码或被替换为 `U+FFFD`。
- 新增或修改文本后运行 `npm run check:utf8`。

## 原始数据只读

- 微信、QQ、聊天附件及用户指定的其他原始数据目录一律只读。
- 禁止删除、移动、重命名或改写原始数据；解密、解析、索引、转码和归档只能读取原件并写入明确的副本或派生产物目录。
- 无法确认某路径是否属于原始数据时，按只读处理并先核实边界。

## 隐私与架构护栏

- 个人聊天、媒体、数据库、密钥、私有环境文件和缓存不得进入 Git。
- 提交前运行 `npm run check:guardrails`；不得通过扩大 baseline 掩盖新违规。
- 新源码文件不得超过 300 行；历史超限文件不得继续增长。

## 阶段工作流

- 每个阶段先明确范围，完成实现后运行与风险相称的测试、类型检查、lint、构建和护栏检查。
- 验证通过并检查 diff、UTF-8 与隐私边界后，每完成一个阶段都要创建一次本地 commit，提交信息说明该阶段的实际产出。
- 未经人类明确要求，不得 push、创建 PR，也不得改写或丢弃用户已有改动。
